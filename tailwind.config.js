module.exports = {
    purge: [
        './src/*.html', './src/*.svelte'
    ],
    important: true,
    darkMode: 'media', // or 'media' or 'class'
    mode: 'jit',
    theme: {
        extend: {},
    },
    variants: {},
    plugins: [],
}

